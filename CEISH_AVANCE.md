# Control de Avance — Prototipo Funcional CEISH (v3)

Este documento registra de forma detallada el progreso, las decisiones de diseño y las tareas completadas durante la implementación del prototipo funcional de la plataforma CEISH bajo la arquitectura de **Motor Configurable de Workflows**.

---

## 🎯 Objetivo General
Construir un prototipo funcional interactivo basado en un **motor de workflows configurable**, donde el administrador pueda definir dinámicamente Tipos de Documento, Secciones/Etapas, Anexos reutilizables y sus correspondientes preguntas. El flujo de "Investigación" pasa a ser la primera configuración semilla del sistema, simulando los datos en Zustand + localStorage.

---

## 📅 Estado de las Tareas (Roadmap de Implementación)

> **Nota (2026-07-09):** las tablas de abajo fueron auditadas línea por línea contra el código real (no contra lo que se planeó). Algunos ítems que estaban marcados `✅ Completado` en la versión anterior de este documento en realidad tenían solo la mitad del trabajo (acción en el store sin pantalla que la use), así que se corrigió su estado a `🟡 Parcial`. Ver el detalle en "Sesión 2" más abajo para la evidencia de cada corrección.
>
> **Nota (2026-07-10):** se implementó el sistema de notificaciones completo (Fase 4.2) y se corrigieron dos bugs de UI (`AppShell.tsx` navegando durante el render, y el panel de notificaciones recortado por `overflow: hidden` del sidebar). En paralelo, otro colaborador (`Xmarcelo-05`) eliminó la pantalla dedicada de Asignaciones y consolidó esa función dentro de `AdminDashboard.tsx` (rama fusionada en `0da0850`). Ver "Sesión 4" para el detalle con evidencia.

### 🧱 Fase 1: Cimientos y Capa de Datos (Generalizado)
| Tarea | Estado | Descripción / Entregable |
|---|---|---|
| **1.1 Definición de Tipos** | ✅ Completado | Modelos generalizados (`Documento`, `RespuestaAnexo`, `TipoDocumento`, `Seccion`, `Pregunta`, `Autor`, `Escalamiento`, `Notificacion`) en [platform.types.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/shared/types/platform.types.ts). Incluye alias de compatibilidad (`Investigacion`, `EmisionAnexo`, `InvestigacionEstado`). |
| **1.2 Acciones CRUD de Configuración y Seed Dinámico** | ✅ Completado | Acciones CRUD creadas en [ceishStore.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts) (`crearAnexoTemplate`, `crearTipoDocumento`, etc.) y seed de "Investigación" (7 anexos, 3 secciones) orquestado secuencialmente a través de esas mismas acciones dentro de `resetearDatos`, tal como pide la sección 14.1 del dominio ("agregar, no reemplazar de golpe"). |
| **1.3 Panel Admin: CRUD de Anexos y Preguntas** | ✅ Completado | Pantalla de gestión interactiva [AnexoTemplateCRUD.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/AnexoTemplateCRUD.tsx) para crear/editar/eliminar anexos y preguntas de los 3 tipos (`checklist`/`texto-libre`/`si-no`, más `cumple-nocumple` como variante añadida). |
| **1.4 Panel Admin: CRUD de Tipos de Documento** | ✅ Completado | Pantalla de gestión interactiva [TipoDocumentoCRUD.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/TipoDocumentoCRUD.tsx) para crear flujos, etapas/secciones y asociar plantillas reutilizables. |

### 📝 Fase 2: Interfaz del Investigador Dinámica
| Tarea | Estado | Descripción / Entregable |
|---|---|---|
| **2.1 Autores por Cédula y Conflicto Automático** | ✅ Completado | Captura de autores por cédula con autocompletado en [CrearInvestigacionModal.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/student/components/CrearInvestigacionModal.tsx), cruzando contra una lista simulada `USUARIOS_REGISTRADOS` para detectar evaluadores en conflicto. |
| **2.2 Formulario de Llenado Dinámico** | ✅ Completado | Renderizado dinámico de los anexos obligatorios/opcionales de la Etapa 1 según el `TipoDocumento`, tanto en el wizard de creación como después en [SubmissionPage.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/student/SubmissionPage.tsx) (navegación libre por pestañas, sin bloqueo secuencial). |
| **2.3 Subida de Correcciones en Rondas** | ✅ Completado | Caja de subida de PDF correctivo y justificación en `SubmissionPage.tsx` cuando el revisor devolvió con observaciones (`con-observaciones`) en estado `'revision-tecnica'`. |
| **2.4 Dashboards separados (todas las revisiones vs. pendientes)** | 🟡 Parcial — no cumple la especificación | El dominio (sección 14, punto 6) pide **dos vistas independientes**. `SubmissionPage.tsx` es una única vista (tabla + panel de detalle lateral); no hay una pantalla separada de "tareas pendientes/devueltas". La información está disponible, pero no como vistas independientes. |

### 🔍 Fase 3: Interfaz del Revisor Dinámica
| Tarea | Estado | Descripción / Entregable |
|---|---|---|
| **3.1 Dashboard de Evaluaciones Ciega** | ✅ Completado | 3 pestañas (Pendientes/Suspendidas/Completadas) con contadores dinámicos, filtradas por asignación activa del evaluador y ocultación estricta de identidad de autores, en [EvaluatorDashboard.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/EvaluatorDashboard.tsx). |
| **3.2 Evaluación Dinámica Split-Screen** | ✅ Completado | Carga dinámica de pestañas de anexos y preguntas según la sección activa del trámite (`activeSeccion.anexos`, no hardcodeado a Anexo 11→12). Integración con `<PDFViewer>` desde `ceishFileCache` (memoria de sesión) en [ReviewCeishPage.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/components/ReviewCeishPage.tsx). |
| **3.3 Historial de Rondas y Anotaciones por Página** | ✅ Completado | Anotaciones por página del PDF ligadas al Anexo 12 (`ComentarioAnotacion`) y panel de rondas anteriores con observaciones (`rondasPreviasA12`) en `ReviewCeishPage.tsx`. |
| **3.4 Disparadores de Acciones Especiales** | ✅ Completado (con la limitación reconocida en el dominio) | Acciones programadas a mano por `anexoTemplateId` literal (`'anexo-27'`, `'anexo-11'`, `'anexo-23'`, `'anexo-12'`, `'anexo-13'`, `'anexo-26'`) en `ReviewCeishPage.tsx` y `ceishStore.ts` — el propio código las marca con un comentario explícito de "limitación consciente del prototipo": si el admin crea un `TipoDocumento` nuevo con otros anexos, estos disparadores no se activan automáticamente y habría que programarlos a mano, tal como anticipa la sección 3 del dominio. |
| **3.5 Devolución y Escalamiento al Admin** | 🟡 Parcial | El evaluador **sí puede** devolver (modal "Devolver para Correcciones", regresa el documento a `'creada'`) y **sí puede** escalar (modal "Escalar a Admin" → `crearEscalamiento`, [ceishStore.ts:1219](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)). Desde la Sesión 4, escalar **sí notifica automáticamente a todos los admins** (Fase 4.2). Lo que sigue faltando es la bandeja de resolución del lado del administrador (ver 4.1). |

### 🛠️ Fase 4: Motor de Escalamientos, Notificaciones y Auditoría
| Tarea | Estado | Descripción / Entregable |
|---|---|---|
| **4.1 Bandeja de Admin para resolver Escalamientos** | ⬜ No implementado | `resolverEscalamiento(id, edicionAdmin)` existe en `ceishStore.ts` (línea 1230) y actualiza el estado a `'resuelto'`, pero **ningún componente lo invoca** (confirmado de nuevo en la Sesión 4 con `grep` sobre todo `src/`). No existe pantalla de admin para ver escalamientos pendientes ni para editar el anexo escalado (sección 8 del dominio). Sigue siendo el único punto pendiente de la Fase 4. |
| **4.2 Notificaciones (automáticas + mensajería manual)** | ✅ Completado | Implementado en la Sesión 4: helper de fan-out `buildNotificaciones` ([ceishStore.ts:41](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) enganchado en los 9 eventos del dominio (cambio de etapa, revisión exitosa, observaciones, documento regresado, asignación de evaluador, conflicto de interés → admin, escalamiento → admin, y el punto 5 dividido en edición de anexo/plantilla vs. edición directa del documento). Mensajería manual multi-destinatario en `enviarNotificacionManual` ([ceishStore.ts:1207](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) + pantalla `/admin/notificaciones` ([NotificacionesAdminCRUD.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/NotificacionesAdminCRUD.tsx)). Campanita + badge + bandeja desplegable compartida por los 3 roles en `AppShell.tsx`. Verificado end-to-end con un smoke test en Playwright contra el dev server real. **Sesión 5:** el mensaje de "documento modificado por el admin" ahora detalla puntualmente qué campo(s) cambiaron (título, riesgo declarado/confirmado, estado) con valor anterior → nuevo, vía `describirCambiosDocumento` ([ceishStore.ts:73](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)), en vez del mensaje genérico original. |
| **4.3 Congelamiento total (tabla de la sección 7 del dominio)** | 🟡 Parcial | Implementado: eliminar/editar el texto de una pregunta borra la respuesta asociada en documentos activos y genera notificación automática (`editarAnexoTemplate`/`eliminarAnexoTemplate` en `ceishStore.ts`); documentos `aprobada`/`anulada` quedan excluidos de esa limpieza (congelados). Desde la Sesión 4 esa notificación ya no depende de que exista una respuesta previa: se calcula por **todos los documentos activos que usan la plantilla** (vía `TipoDocumento.secciones.anexos`), no solo los que ya la habían respondido, y llega tanto al investigador como a los evaluadores activos del documento. Cada `RespuestaAnexo` guarda su `snapshotPreguntas`. **No verificado como regla explícita:** no existe un campo booleano de "sección completa" que se revierta a incompleta — el estado se recalcula al vuelo (`isEtapa1Completa`/`isAnexoCompletado`) cada render, lo cual logra el mismo efecto observable pero no es la implementación literal descrita. |
| **4.4 Consolidación de Asignaciones en AdminDashboard** | ✅ Completado (por otro colaborador) | La pantalla dedicada `/admin/asignaciones` (`AssignmentPanel.tsx`) fue retirada de las rutas y del sidebar; su función se absorbió dentro de `AdminDashboard.tsx` como pestaña **"Proyectos Registrados"**, cuyo modal "Editar Proyecto" ya permite reasignar evaluador (llama a `editarDocumento`, que ahora también dispara notificaciones — ver 4.2). `AssignmentPanel.tsx` quedó huérfano en disco (sin imports en ningún lado); candidato a borrado. **Detalle a validar:** la pestaña "Carga de Evaluadores" del mismo dashboard sigue leyendo del sistema legado (`platformService.getAssignments()`/`getAllSubmissions()`, tipos `Assignment`/`StudentSubmission`) y navega a la ruta legacy `/evaluador/revision/:submissionId`, no al dominio CEISH v3 (`documentos`/`asignaciones` de `ceishStore`) — el dashboard hoy mezcla ambos sistemas en pestañas distintas. |

---

## 📝 Registro de Sesiones e Hilos de Trabajo

### Sesión 1: Alineación del Repositorio y Diseño del Plan
* **Fecha:** 2026-07-08
* **Actividades:**
  * Verificación directa en el código de los mecanismos de autenticación (sin JWT) y backend (Vite middleware).
  * Confirmación del esquema activo en PostgreSQL (`database/schema.sql`).
  * Clasificación de archivos legacy frente a los nuevos que se van a agregar.
  * Creación del archivo de seguimiento [CEISH_AVANCE.md](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/CEISH_AVANCE.md).
  * **Tarea 1.1 completada:** Adición de tipos TypeScript específicos para el Motor Configurable en [platform.types.ts](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/shared/types/platform.types.ts).
  * **Tareas 1.2, 1.3 y 1.4 completadas:** Implementación de acciones CRUD dinámicas y seed atómico por helpers puros en el store de Zustand [ceishStore.ts](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts). Creación y estilizado de las pantallas de administración interactiva [AnexoTemplateCRUD.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/AnexoTemplateCRUD.tsx) y [TipoDocumentoCRUD.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/TipoDocumentoCRUD.tsx). Integración de rutas en el panel y sidebar de navegación de administrador en [AppShell.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/shared/components/AppShell.tsx) y en el [router](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/app/router/index.tsx).
  * **Tareas 2.1, 2.2 y 2.3 completadas:** Migración de la interfaz del Investigador. Autocompletado de co-autores por cédula con detección de conflictos en [CrearInvestigacionModal.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/student/components/CrearInvestigacionModal.tsx). Formulario de llenado dinámico de anexos (1 a 9) condicionado a obligatoriedad para el envío de revisión, y adición del panel de subida de PDF correctivo ante observaciones técnicas metodológicas en [SubmissionPage.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/student/SubmissionPage.tsx).
  * **Mejoras de Integración de Flujo (Solicitud de Usuario):**
    * Integración del llenado de formularios de la Etapa 1 directamente en [CrearInvestigacionModal.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/student/components/CrearInvestigacionModal.tsx) a través de un wizard con barra de navegación horizontal por pestañas y validación de obligatoriedad antes de registrar. Selección dinámica del tipo de documento a partir de los flujos configurados por el admin.
    * Transición directa al estado de Estratificación (Etapa 2) con asignación ciega de evaluador al registrar el proyecto.
    * Organización del panel del Evaluador en [EvaluatorDashboard.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/EvaluatorDashboard.tsx) en 3 pestañas: Pendientes (revisiones activas), Suspendidas (proyectos anulados) y Completadas (proyectos aprobados) con contadores dinámicos.
    * Adición de una pestaña de lectura **"Llenado Investigador (Etapa 1)"** en [ReviewCeishPage.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/components/ReviewCeishPage.tsx) para que el evaluador pueda auditar las respuestas del investigador.
    * Corrección de maquetación en la pantalla de evaluación dividida en [ReviewCeishPage.tsx](file:///C:/Users/PC/Desktop/CHEISH%20Prototype/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/components/ReviewCeishPage.tsx), alineando el visor de PDF al lado izquierdo (`eval-pdf-panel`) y el panel de criterios al derecho (`criteria-panel`).
    * Solución de usabilidad agregando barras de desplazamiento vertical automáticas (`overflowY: 'auto'`) al panel de revisión y líneas divisoras claras con sombreado lateral para evitar redimensionar o alejar el zoom de la pantalla.
    * Implementación de la lógica de **roles acumulativos/heredados** en [AppShell.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/shared/components/AppShell.tsx) permitiendo a Revisores y Administradores acceder fluidamente a las funciones de las jerarquías inferiores (ej. Revisor puede registrar e investigar sus propios proyectos, y Admin puede evaluar y revisar asignaciones).

### Sesión 2: Auditoría de Estado Real contra el Dominio v3
* **Fecha:** 2026-07-09
* **Motivo:** Se pidió analizar el proyecto junto con `CEISH_DOMINIO_Y_FLUJO.md` y actualizar este documento a detalle con lo implementado hasta ahora. Se leyó el código completo del motor (tipos, store, y las pantallas de las 3 pestañas) en vez de asumir lo que decía la Sesión 1, y se encontraron varias afirmaciones de "Completado" que en realidad correspondían solo a la mitad del trabajo.
* **Correcciones de estado realizadas en las tablas de arriba** (con evidencia de archivo:línea):
  * **Escalamiento a Admin:** `resolverEscalamiento` está definido y funcional en [ceishStore.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts) (líneas 885-896), pero un `grep` sobre todo `src/` confirma que **ninguna pantalla lo invoca** — el admin no tiene bandeja de escalamientos. Solo el lado del evaluador (`crearEscalamiento`, modal "Escalar a Admin" en `ReviewCeishPage.tsx` líneas 334-359) está terminado. Se corrigió de "✅ Completado" a la nueva Fase 4 (pendiente).
  * **Notificaciones:** `crearNotificacion`/`marcarNotificacionLeida` existen y se disparan automáticamente desde `editarAnexoTemplate` (línea 335-343 de `ceishStore.ts`) cuando una edición de pregunta borra respuestas ya dadas, pero no hay ningún componente de UI (campanita, bandeja, contador de no leídas) en ninguna de las 3 interfaces de rol. Nunca estuvo listado como tarea en la Sesión 1, así que se agrega ahora como pendiente explícito (Fase 4.2), porque el dominio lo pide en la sección 6 y 8.
  * **Dashboards separados del investigador (punto 6 del roadmap del dominio, sección 14):** el dominio pide dos pantallas independientes ("todas las revisiones con fecha" vs. "tareas pendientes/devueltas"). `SubmissionPage.tsx` implementa una sola vista combinada (tabla + panel de detalle). Nunca se había registrado como pendiente en la Sesión 1; se agrega ahora (Fase 2.4).
  * **Bug de navegación "falta botón de volver atrás" (punto 5 del roadmap del dominio):** revisando `ReviewCeishPage.tsx` completo, no existe un botón explícito "Volver al Dashboard" en el flujo normal de evaluación — solo aparece en la rama de error cuando el documento no existe (línea 83-90). La navegación de vuelta ocurre implícitamente vía `navigate('/evaluador')` al final de cada acción, o mediante el sidebar. No hay evidencia de que este bug reportado en el dominio se haya corregido explícitamente.
  * **Bug de asignación ciega que no aparecía en el panel del evaluador (punto 11 del roadmap del dominio):** en el código actual, `crearDocumento` (líneas 418-520 de `ceishStore.ts`) crea el documento y la asignación **en el mismo `set()`**, de forma sincrónica, y `EvaluatorDashboard.tsx` filtra directamente por `asignaciones.filter(a => a.evaluadorId === currentUser.id)`. Estructuralmente no debería reproducirse el bug descrito, pero no se encontró un commit ni comentario que documente la causa raíz ni una prueba manual registrada — se deja como "probablemente resuelto por diseño, sin confirmación explícita".
* **Hallazgos adicionales no cubiertos por la Sesión 1 (documentados, no son bugs sino comportamientos deliberados del prototipo):**
  * El almacenamiento de archivos PDF es 100% en memoria del navegador (`ceishFileCache` en [fileCache.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/fileCache.ts)), con un comentario explícito en el código explicando que esto evita serializar binarios en `localStorage`. Efecto observable: **recargar la página pierde todos los PDFs cargados en la sesión** (tanto para investigador como evaluador), y ambas pantallas muestran un mensaje de fallback explicándolo al usuario.
  * `CrearInvestigacionModal.tsx` ya asigna evaluador ciego y transiciona a `'estratificacion'` dentro de la misma llamada a `crearDocumento` (no espera a un `solicitarRevision` posterior). Sin embargo, `SubmissionPage.tsx` conserva un botón "Solicitar Revisión Ética" para documentos que quedan en estado `'creada'` (caso borde: si no hay evaluadores disponibles sin conflicto al momento de crear). Ambos caminos coexisten sin conflicto, pero es una duplicación a tener en cuenta si se refactoriza este flujo más adelante.
  * Los disparadores de negocio por anexo (`anexo-27`, `anexo-11`, `anexo-23`, `anexo-12`, `anexo-13`, `anexo-26`) están *hardcodeados por ID literal* tanto en `ceishStore.ts` como en `ReviewCeishPage.tsx`. El propio código lo señala con un bloque de comentario explícito como "limitación consciente del prototipo" — coincide exactamente con lo que la sección 3 y el punto 12 del dominio anticipan ("se programan a mano, caso por caso"), así que no es una desviación, es la implementación esperada de esa regla.
* **Estado global honesto tras esta auditoría:** el motor configurable (Fases 1-3 del roadmap) está sólidamente implementado y usable de punta a punta para el flujo semilla "Investigación". Lo que falta para cerrar completamente la v3 del dominio es exclusivamente el lado de administración de la sección 8 (bandeja de escalamientos), las notificaciones (sección 6), y dos mejoras de UX ya reportadas en el dominio (dashboards separados del investigador, botón de volver atrás del evaluador).

### Sesión 3: Simplificación de Tipos de Campo de Pregunta + Nuevo Tipo "Archivo"
* **Fecha:** 2026-07-09
* **Motivo:** A pedido explícito, se simplifican los tipos de campo disponibles al crear una pregunta de un `AnexoTemplate`. Antes de decidir el alcance se preguntó si la eliminación de `si-no`/`cumple-nocumple` debía ser solo cosmética en el selector del CRUD o una eliminación completa del sistema de tipos (lo cual afecta a los 7 anexos semilla y su renderizado en 3 pantallas distintas); se eligió la opción de eliminación completa.
* **Cambio de `CampoTipo`** en [platform.types.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/shared/types/platform.types.ts) línea 94: de `'checklist' | 'cumple-nocumple' | 'comentarios-solo' | 'texto-libre' | 'si-no'` a `'checklist' | 'texto-libre' | 'archivo'`. Se eliminó también `'comentarios-solo'`, que ya era un tipo muerto (nunca aparecía como opción seleccionable en el CRUD).
* **Migración de los 7 anexos semilla** en [ceishStore.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts) (`resetearDatos`): todas las preguntas que usaban `si-no` (Anexos 2-9) o `cumple-nocumple` (Anexos 27, 11, 23, 12, 13, 26) pasaron a `checklist`. Efecto observable: esas preguntas ahora se muestran como un único checkbox de conformidad en vez de un par de radios Sí/No — mismo dato booleano subyacente, distinta interacción.
* **CRUD de plantillas** [AnexoTemplateCRUD.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/AnexoTemplateCRUD.tsx): el selector de "Tipo de Campo" al agregar/editar una pregunta ahora solo ofrece `Checklist`, `Respuesta Abierta (Texto)` y `Adjuntar Archivo (Imagen o PDF)`. El valor por defecto de una pregunta nueva cambió de `cumple-nocumple` a `checklist`.
* **Nuevo tipo "archivo" implementado end-to-end** (no solo en el selector del admin, sino también en las 3 pantallas donde se responde/lee una pregunta, porque quitar los tipos viejos del sistema de tipos obligaba a tocar esas comparaciones de todas formas):
  * [CrearInvestigacionModal.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/student/components/CrearInvestigacionModal.tsx) (wizard Etapa 1) y [SubmissionPage.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/student/SubmissionPage.tsx) (llenado posterior del investigador): input de archivo que valida PDF/imagen, lo guarda en `ceishFileCache` con una key generada, y persiste `{ documentName, documentPath }` como `valor` de la pregunta (mismo patrón que `VersionArchivo`).
  * [ReviewCeishPage.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/evaluator/components/ReviewCeishPage.tsx): mismo input de archivo en el formulario dinámico del evaluador, y la pestaña de solo-lectura "Llenado Investigador (Etapa 1)" ahora muestra un botón "Ver" para preguntas de tipo archivo en vez de intentar volcar el objeto como texto.
  * `SubmissionPage.tsx` (panel "Anexos Emitidos"): se agregó una rama para mostrar el nombre del archivo adjunto con botón "Ver" cuando la pregunta emitida es de tipo `archivo`.
  * Los archivos de preguntas comparten la misma limitación ya documentada del PDF principal: viven en `ceishFileCache` (memoria del navegador) y se pierden al recargar la página.
* **Verificación:** `npx tsc -b` corrió sin errores tras el cambio (ambos proyectos, app y node).

### Sesión 4: Sistema de Notificaciones (Fase 4.2) + Fixes de UI + Auditoría de la Fusión con `develop`
* **Fecha:** 2026-07-10
* **Motivo:** Cerrar la Fase 4.2 del roadmap (pendiente desde la Sesión 2). Se pidió explícitamente un diseño primero: quién recibe cada evento, de dónde sale cada destinatario, y solo después implementar. Dos ambigüedades del punto 5 del dominio ("modificación del documento por el administrador") se resolvieron por pregunta directa antes de tocar código: (a) el conflicto de interés del evaluador se detecta al emitir un anexo con `resultado:'conflicto-interes'` (reutiliza el dato ya existente en vez de agregar una acción nueva), y (b) "documento regresado" notifica **solo al investigador**, no también al evaluador que ejecuta la devolución.

* **Diseño de destinatarios** (sin cambios de esquema en `Notificacion` — se resuelve con fan-out, una fila por destinatario):
  * Investigador → `documento.investigadorId`.
  * Evaluador(es) → `asignaciones` activas filtradas por `documentoId`.
  * Admin → constante `ADMIN_IDS` ([ceishStore.ts:39](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)), mismo esquema de UUID determinista que `database/seed.sql` — se notifica a **todos** los admins (no existe hoy un concepto de "admin dueño del trámite").

* **Los 9 disparadores automáticos y dónde quedaron enganchados** (todo vía el helper de fan-out `buildNotificaciones`, [ceishStore.ts:41](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)):
  1. **Cambio de etapa → investigador**: `emitirAnexo` ([ceishStore.ts:916](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) cuando `nuevoEstado` cambia y no es `aprobada`/`creada`/`anulada`; también en `crearDocumento` ([:587](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) y `solicitarRevision` ([:802](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) cuando la asignación ciega mueve el trámite a Estratificación.
  2. **Revisión exitosa → investigador**: `emitirAnexo` cuando `nuevoEstado === 'aprobada'`.
  3. **Observaciones → investigador**: `emitirAnexo` cuando `resultado === 'con-observaciones'` (puede coexistir con el punto 4 en la misma llamada, ej. "Devolver para Correcciones").
  4. **Documento regresado → investigador (solo)**: `emitirAnexo` cuando `nuevoEstado === 'creada'` viniendo de otro estado.
  5. **Modificación por admin, dividida en dos según lo pedido**:
     * **5a — edita el anexo/plantilla → investigador + evaluadores de *todos* los documentos activos que la usan**: `editarAnexoTemplate` ([:428](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) y `eliminarAnexoTemplate` ([:508](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)). Se reemplazó la notificación parcial que ya existía (solo notificaba a quien ya había respondido) por un cálculo sobre `TipoDocumento.secciones.anexos` que encuentra **todos** los documentos activos que referencian la plantilla, cubriendo también el caso de agregar una pregunta nueva que nadie había respondido aún.
     * **5b — edita el documento puntual → investigador + evaluador(es) de ese documento**: `editarDocumento` ([:703](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)), que ya existía para que el admin cambie tema/descripción/riesgo/estado y reasigne evaluador.
  6. **Asignación de evaluador → evaluador**: `crearDocumento`, `solicitarRevision`, `darseDeBajaRevisor` ([:1030](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) y `editarDocumento` (cuando el admin reasigna manualmente).
  7. **Conflicto de interés del evaluador → admin**: `darseDeBajaRevisor`, en el mismo punto donde ya se emitía el Anexo 23 con `resultado:'conflicto-interes'`.
  8. **Escalar a admin → admin**: `crearEscalamiento` ([:1219](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) — el campo `Escalamiento.notificado` ya existía en el tipo desde la Sesión 1 pero nunca se había usado; sigue sin usarse para *esto* (es para el otro lado, cuando el admin resuelve), pero ahora al menos el admin se entera de que hay un escalamiento pendiente.

* **Mensajería manual del admin**: nueva acción `enviarNotificacionManual(destinatarioIds, mensaje)` ([:1207](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) y nueva pantalla `/admin/notificaciones` → [NotificacionesAdminCRUD.tsx](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/features/admin/components/NotificacionesAdminCRUD.tsx): selección multi-destinatario por usuario individual o por rol completo (checkbox "seleccionar/deseleccionar todos" por rol), mensaje libre, e historial de mensajes ya enviados con estado leído/no leído.

* **UI de campanita y bandeja** en `AppShell.tsx`: ícono con badge de no leídas (máx. mostrado `9+`) compartido por los 3 roles, dropdown con lista ordenada por fecha descendente, clic en una notificación la marca como leída (`marcarNotificacionLeida`).

* **Dos bugs de UI encontrados y corregidos durante la verificación manual** (no estaban en el roadmap, se descubrieron probando el flujo, no eran parte del pedido original de notificaciones):
  * **`AppShell.tsx` navegaba a `/login` durante el render** (`if (!currentUser) { navigate(...); return null; }` fuera de un efecto) cuando `currentUser` se volvía `null` tras una recarga de página (el botón "Restablecer prototipo" hace `resetearDatos()` + `window.location.reload()`, y `authStore` no persiste la sesión). React ignora el `navigate()` en ese punto del ciclo de vida, así que la app quedaba en una pantalla en blanco en vez de redirigir. Se movió la llamada a un `useEffect([currentUser, navigate])`. Verificado con Playwright: el botón de reset ahora sí redirige solo a `/login`.
  * **El panel de notificaciones quedaba cortado contra el borde del sidebar.** Causa: `.shell__sidebar` tiene `overflow: hidden` y el panel era `position: absolute`, así que cualquier parte que sobresaliera de los 220px del sidebar se recortaba. Se cambió a `position: fixed` con `top`/`left` calculados en JS desde `getBoundingClientRect()` de la campanita (los elementos `fixed` no se recortan por el `overflow:hidden` de un ancestro sin `transform`). Verificado con captura de pantalla tras el fix: el panel se dibuja completo.

* **Verificación**: `npx tsc -b` sin errores. Smoke test con Playwright contra el dev server real (Postgres + MinIO en Docker, no mocks): como admin se editó el Anexo 1 (dispara 5a) y se envió un mensaje manual a Juan Pérez; al loguearse como Juan Pérez el badge mostró 2 no leídas con el contenido correcto de ambos mensajes, y bajó a 1 al marcar una como leída.

* **Auditoría de la fusión con `develop` (commit `0da0850`):** mientras se trabajaba en notificaciones, otro colaborador (`Xmarcelo-05`, commit `fdafe2b`) modificó `ceishStore.ts`, `AppShell.tsx` y `router/index.tsx` en paralelo para eliminar la ruta `/admin/asignaciones` y absorber esa función dentro de `AdminDashboard.tsx` (ver Fase 4.4 arriba). Tras la fusión (`0da0850`) se verificó explícitamente que ningún cambio de esa rama pisara el trabajo de notificaciones: los 14 puntos de uso de `buildNotificaciones` siguen presentes, `npx tsc -b` sigue limpio, y `AssignmentPanel.tsx` quedó huérfano en disco (nadie lo importa) en vez de eliminado — pendiente decidir si se borra.

### Sesión 5: Notificaciones Personalizadas — Detalle de Campos Modificados
* **Fecha:** 2026-07-10
* **Motivo:** El admin pidió que las notificaciones fueran "más personalizadas" — puso como ejemplo que cuando el admin modifica un documento, el mensaje indique **qué parte cambió**, en vez del genérico `"El Administrador modificó el proyecto {codigo}."` que dejó la Sesión 4.
* **Cambio en [ceishStore.ts](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts):**
  * Se agregaron mapas de etiquetas legibles `ESTADO_LABELS`/`RIESGO_LABELS` (línea ~57) y una función pura `describirCambiosDocumento(doc, campos)` (línea ~73) que compara el `Documento` original contra los `campos` entrantes de `editarDocumento` y arma una lista de strings tipo `"título: \"A\" → \"B\""`, `"estado: Borrador → Estratificación"`, etc. Para `descripcion` (texto largo/justificación) solo se indica que cambió, sin volcar el texto completo en la notificación.
  * `editarDocumento` (5b) ahora usa esa lista para armar el mensaje: `El Administrador modificó el proyecto {codigo}: {cambios.join('; ')}.` en vez del texto genérico. Ya no se dispara si `campos` no tiene cambios reales respecto al documento (antes se disparaba con solo `Object.keys(campos).length > 0`, aunque el valor fuera igual al existente).
* **Alcance de esta sesión:** primero solo se tocó el mensaje de "documento modificado por admin" (5b), que fue el ejemplo explícito del pedido. El usuario luego pidió aplicar lo mismo al mensaje de "anexo/plantilla modificado" (5a), así que se hizo en la misma sesión (ver más abajo). El mensaje de "escalamiento"/"asignación" quedó con su redacción genérica de la Sesión 4 — no se tocó porque no fue pedido.
* **Verificación (5b):** `npx tsc -b` sin errores. Smoke test con Playwright: como admin se editó el proyecto `CEISH-2026-0001` (nuevo título, estado a Estratificación, riesgo confirmado a Riesgo Mínimo) desde la pestaña "Proyectos Registrados" de `AdminDashboard.tsx`; al loguearse como Juan Pérez (el investigador), la notificación en la campanita mostró textualmente: `El Administrador modificó el proyecto CEISH-2026-0001: título: "Uso de pantallas y desarrollo lingüístico en infantes" → "Nuevo tema de prueba QA"; riesgo confirmado: sin confirmar → Riesgo Mínimo; estado: Borrador → Estratificación.`

* **Extensión a "anexo/plantilla modificado" (5a), a pedido explícito de seguimiento ("si, hazlo también"):**
  * Se agregó `CAMPO_TIPO_LABELS` (etiquetas legibles para `CampoTipo`) y `describirCambiosPreguntas(preguntasAntes, preguntasDespues)` ([ceishStore.ts:109](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)), que compara las preguntas de un `AnexoTemplate` antes/después de una edición y describe cada pregunta agregada (`pregunta agregada: "..."`), eliminada (`pregunta eliminada: "..."`), con texto editado (`pregunta editada: "A" → "B"`), o con tipo de campo cambiado (`tipo de campo cambiado en "...": Checklist → Respuesta Abierta`).
  * `editarAnexoTemplate` ([ceishStore.ts:~549](file:///C:/Users/Jesus/Desktop/CEISH/CEISH-PLATFORM-PROTOYPE/src/store/ceishStore.ts)) reemplazó el booleano `huboCambioEstructural` (que solo decía sí/no) por esta lista de cambios, y el mensaje pasó de `"El Administrador modificó las preguntas del Anexo N (Nombre)... Revisa si tus respuestas siguen vigentes."` a `"El Administrador modificó el Anexo N (Nombre) en el proyecto {codigo}: {cambios.join('; ')}."`.
  * `eliminarAnexoTemplate` (elimina la plantilla completa) se dejó con su mensaje a nivel de anexo (ya identifica cuál se eliminó) — no tiene un "diff" de preguntas que aplique, porque desaparece entero.
  * **Verificación:** `npx tsc -b` sin errores. Smoke test con Playwright: como admin se editó el Anexo 1 (texto de la primera pregunta + una pregunta nueva agregada); al loguearse como Juan Pérez la notificación mostró: `El Administrador modificó el Anexo 1 (Solicitud de Revisión Técnica) en el proyecto CEISH-2026-0001: pregunta editada: "Título descriptivo del proyecto de investigación" → "Titulo modificado por QA"; pregunta agregada: "Pregunta nueva agregada por QA".`
